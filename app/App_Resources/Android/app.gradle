// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.runewather.weddingstore"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
